<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="flab.docdoc.hospital.repository.HospitalRepository">

    <insert id="save" parameterType="flab.docdoc.hospital.domain.Hospital" useGeneratedKeys="true" keyProperty="uniqueId" keyColumn="unique_id">
        INSERT INTO hospital (
            business_name,
            destrict,
            city,
            detail_location)
            VALUES
            (
            #{businessName},
            #{destrict},
            #{city},
            #{detailLocation}
            )
    </insert>


    <select id="findByUniqueId" parameterType="java.lang.Long" resultType="flab.docdoc.hospital.domain.Hospital">
        SELECT
            unique_id,
            business_name,
            destrict,
            city,
            detail_location
        FROM hospital
        WHERE unique_id = #{uniqueId}
    </select>

    <update id="update" parameterType="flab.docdoc.hospital.domain.Hospital">
        UPDATE hospital
        SET
            business_name = #{businessName},
            destrict = #{destrict},
            city = #{city},
            detail_location = #{detailLocation}
        WHERE unique_id = #{uniqueId}

    </update>





</mapper>